{% set default_target = "presearch.com" %}

<!DOCTYPE html>

<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="generator" content="sanity">
    <meta http-equiv="Content-Language" content="en">
    <meta name="robots" content="index, follow">

    <title>no-bs.ru</title>
    <meta name="description" content="{# TODO: describe #}" />
    <link rel="canonical" href="https://no-bs.ru" />
    <link rel="icon" type="image/png" href="/favicon.png" /> {# TODO: add an icon #}
    <link rel="stylesheet" href="/style.css" />
    <script src="/generator.js"></script>
</head>

<body class="rows nowrap nogap">
    <header>
        <a target="_blank" href="https://github.com/nonk123/no-bs" id="title">no-bs.ru</a>
    </header>
    <main>
        <h1>no-bs utilities</h1>
        <h2 id="values-yml-generator">values.yml generator</h2>
        <div class="cols">
            <div id="values-yml-inputs">
                <label for="host">Server host</label>
                <input id="host" type="text" placeholder="Node's IP address">
                <label for="port">Inbound port</label>
                <input id="port" type="number" placeholder="443" value="443">
                <div>
                    <label for="private-key">Private key</label>
                    <button onclick="randomizeKeyPair()">🔄</button>
                </div>
                <input id="private-key" type="text" placeholder="REALITY private key">
                <label for="public-key">Public key</label>
                <input id="public-key" type="text" placeholder="REALITY public key">
                <label for="reality-target">REALITY spoofing target</label>
                <input id="reality-target" type="text" placeholder="{{ default_target }}:443"
                    value="{{ default_target }}:443">
                <label for="reality-server-name">REALITY TLS domain name</label>
                <input id="reality-server-name" type="text" placeholder="{{ default_target }}"
                    value="{{ default_target }}">
            </div>
            <div id="values-yml-outputs" class="rows nogap">
                <label for="values-yml-output"><code>values.yml</code></label>
                <textarea id="values-yml-output" readonly></textarea>
            </div>
            <div>
                <p><button onclick="addUser()">➕</button> Users:</p>
                <table id="users">
                    <tbody></tbody>
                </table>
            </div>
        </div>
        <h2 id="vless-url-generator">VLESS URL for custom user ID</h2>
        <p>Before use, make sure to fill in the inputs above.</p>
        <div id="custom-vless">
            <label for="custom-id">User ID</label>
            <input id="custom-id" type="text" oninput="updateCustomVlessLink()">
            <label for="custom-vless-link">VLESS link</label>
            <div class="cols nowrap">
                <input id="custom-vless-link" type="text" placeholder="vless://" readonly>
                <button onclick="copyCustomVlessLink()">📋</button>
            </div>
        </div>
    </main>
    <footer>
        <a target="_blank" href="https://github.com/nonk123/no-bs">source code</a>
    </footer>
</body>

</html>
